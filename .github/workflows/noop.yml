name: do nothing useful
on:
  push: {}
  schedule:
  - cron: '0 * * * *'
jobs:
  do-nothing-one:
    name: Do nothing
    steps:
    - uses: 'actions/checkout@v3'
    - run: 'ls -l'
  do-nothing-two:
    name: Do something
    steps:
    - uses: 'actions/checkout@v3'
    - run: 'ls -ltr'